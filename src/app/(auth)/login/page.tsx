// Login page ("/login"). The form is a Client Component wired to the loginAction
// Server Action via useActionState (Lesson 03). Auth.js verifies the credentials.
// Docs: https://nextjs.org/docs/app/guides/forms
//       https://authjs.dev/getting-started/authentication/credentials

export default function LoginPage() {
  // TODO (Milestone 3): <LoginForm /> (client) calling loginAction; show errors.
  return <main>{/* login form */}</main>
}
