// Signup page ("/signup"). Form → signupAction (Server Action): validate, hash
// the password, create the user, start the session (Lesson 03/05).
// Docs: https://nextjs.org/docs/app/guides/forms

export default function SignupPage() {
  // TODO (Milestone 3): <SignupForm /> calling signupAction; pick role (candidate/employer).
  return <main>{/* signup form */}</main>
}
