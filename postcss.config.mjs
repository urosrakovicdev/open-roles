// PostCSS config for Tailwind CSS v4. In v4, Tailwind is a PostCSS plugin and
// configuration lives in CSS (see src/app/globals.css) rather than a JS file.
// Docs: https://tailwindcss.com/docs/installation/framework-guides/nextjs
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
}

export default config
